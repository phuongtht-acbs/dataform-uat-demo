config {
    type: "view",
    schema: "gold",
    name: "transaction",
    description: "Thông tin hiện tại của các chi nhánh",
    tags: ["gold", "transaction", "current"],
    dependOnDependencyAssertions: true
}

SELECT *
FROM ${ref("gold", "transaction_history")}
WHERE is_current = true